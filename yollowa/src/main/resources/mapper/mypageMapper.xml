<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.proj.yollowa.model.mypage.MypageDao">
	<select id="userDetail" resultType="com.proj.yollowa.model.entity.UserVo">
		select * from user where user_number=#{user_number}
	</select>
	<select id="lReservationInfo">
		SELECT * FROM LRESERVINFO 
		WHERE LRSERVINFO_USERNUMBER=#{LRSERVINFO_USERNUMBER}
		 AND LRESERVINFO_RESERVSTATUS=#{LRESERVINFO_RESERVSTATUS} 
		 AND LRESERVINFO_CARTSTATUS=#{LRESERVINFO_CARTSTATUS}
		 
	</select>
	
</mapper>
